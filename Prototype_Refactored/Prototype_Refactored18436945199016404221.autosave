ProgramRunner programRunner;

void setup(){
    size(960, 540, P2D);
    pixelDensity(2);
  
    programRunner = new ProgramRunner();
    init();

}


void draw(){
    background(0);
    run(programRunner.programState);

}
